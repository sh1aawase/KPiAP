<Window x:Class="z1.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Финансы z1" Height="500" Width="750" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid>
        <Border x:Name="LoginPanel" Background="#EFEFEF" Panel.ZIndex="100">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAuthenticated}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="220">
                <TextBlock Text="АВТОРИЗАЦИЯ" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBox Text="{Binding LoginInput, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="28" />
                <PasswordBox x:Name="PassBox" Margin="0,5" Height="28"/>
                <Button Content="ВОЙТИ" Click="Login_Click" Height="35" Margin="0,15" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <TabControl Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVis}}">
            <TabItem Header="Главная">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FFF3CD" CornerRadius="5" Margin="5">
                        <TextBlock Text="{Binding Reminder}" Foreground="#856404" Padding="10" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    </Border>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
                        <GroupBox Header="Новая транзакция" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBox Text="{Binding NewCategory, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="0,0,5,0" />
                                <TextBox Text="{Binding NewAmount, UpdateSourceTrigger=PropertyChanged}" Width="70" Margin="0,0,5,0" />
                                <Button Content="Добавить" Command="{Binding AddTransactionCommand}" Padding="10,0"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Администрирование" BorderBrush="Red">
                            <GroupBox.Style>
                                <Style TargetType="GroupBox">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAdmin}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </GroupBox.Style>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBox x:Name="AdminRemInput" Width="130" Margin="0,0,5,0"/>
                                <Button Content="Обновить" Command="{Binding SaveReminderCommand}" 
                                        CommandParameter="{Binding ElementName=AdminRemInput, Path=Text}" Background="#FFD6D6"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding Transactions}" AutoGenerateColumns="True" IsReadOnly="True" Margin="5"/>
                </Grid>
            </TabItem>

            <TabItem Header="Чат">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListBox ItemsSource="{Binding ChatMessages}" Margin="5" Background="#F9F9F9"/>
                    <DockPanel Grid.Row="1" Margin="5">
                        <Button Content="Отправить" Click="Clear_Click" Command="{Binding SendCommand}" 
                                CommandParameter="{Binding ElementName=ChatIn, Path=Text}" DockPanel.Dock="Right" Width="90"/>
                        <TextBox x:Name="ChatIn" Height="30" VerticalContentAlignment="Center"/>
                    </DockPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>